class Solver {
    /*
     * Class that implements Cube-solving functionality.
     */
    
    
    /*
     * The Solver may load large data structures (e.g., hash tables) into memory.
     * Therefore, it is recommended to never instantiate a Solver and instead use
     * the shared singleton.
     */
    static let shared = Solver()
}
