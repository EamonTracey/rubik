import AVFoundation
import SwiftUI

struct CameraView: View {
    var body: some View {
        Text("Cube!")
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}
